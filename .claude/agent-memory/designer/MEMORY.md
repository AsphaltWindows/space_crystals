# Designer Agent Memory

## Forum Vote Format
- Close votes MUST be just `- designer` with no extra text after the agent name
- The parser only recognizes `- agent_name` — colons, comments, brackets all break it
- Previously made this mistake by adding `- designer: comment` — was invisible to the parser
